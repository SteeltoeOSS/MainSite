@page "/service-discovery/docs"
@page "/steeltoe-discovery/docs"
@page "/docs/steeltoe-discovery"
@page "/discovery"
@page "/discovery/{Area}"

<Docs Component="discovery" Page="@(string.IsNullOrEmpty(Area) ? "discovery-overview.html" : Area+".html")" />
@code {
	[Parameter]
	public string Area { get; set; }
}/>