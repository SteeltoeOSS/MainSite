
@page "/logging/docs"
@page "/steeltoe-logging/docs"
@page "/docs/steeltoe-logging"
@page "/logging/{Area}"

<Docs Component="logging" Page="@(string.IsNullOrEmpty(Area) ? "logging-overview.html" : Area+".html")" />
@code {
	[Parameter]
	public string Area { get; set; }
}